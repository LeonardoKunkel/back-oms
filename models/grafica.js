/* export class GaficaData {
     
     valores = 10;

    constructor() {}

    getDataGrafica() {
        return [
            {name: 'Punto Uno', value: this.valores}
        ]
    }

    incrementarValor(mes, valor) {
        mes = mes.toLowerCase().trim();

        for(let i in this.meses) {
           if (this.meses[i] === mes) {
               this.valores[i] += valor;
           } 
        }

        return this.getDataGrafica();
    }
} */

